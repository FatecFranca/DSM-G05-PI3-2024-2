{
  "name": "DSM-G05-PI3-2024-2",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run serve --prefix front-end\" \"npm run start --prefix back-end\"",
    "atualizar": "npm install && (cd front-end && npm install) && (cd back-end && npm install)",
    "build": "(cd front-end && npm run build)"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "chart.js": "^4.4.6"
  }
}
